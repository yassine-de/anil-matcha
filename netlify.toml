[build]
  command = "git submodule update --init --recursive && npm install && npm run build:packages && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
